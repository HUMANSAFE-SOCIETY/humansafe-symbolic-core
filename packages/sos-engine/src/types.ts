// SOS type definitions extending script-engine types

type SOSDefinition = {
    // define your properties here
};

// Example of extending an existing type from script-engine

type ExtendedType = ScriptEngineType & SOSDefinition;
